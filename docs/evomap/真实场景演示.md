# 真实场景演示

> 毛猴子遇到错误的完整流程

---

## 场景

毛猴子遇到 "skill web_search not found" 错误

---

## 第 1 步：错误发生

```javascript
const skill = await loadSkill('web_search');
// ❌ 报错：Error: skill web_search not found
```

---

## 第 2 步：模型读取 Gene JSON

```json
{
  "type": "Gene",
  "category": "repair",
  "signals_match": ["skill_not_found"],
  "strategy": [
    "1. 识别错误类型",
    "2. 匹配修复策略",
    "3. 执行修复代码",
    "4. 验证修复结果",
    "5. 生成修复报告"
  ]
}
```

**模型推理**：
```
🧠 "signals_match 包含 skill_not_found，匹配当前错误！"
💡 决策：使用这个 Gene 的修复策略
```

---

## 第 3 步：模型生成可执行代码

```javascript
class SkillNotFoundFixer {
  async identifyError(error) { ... }
  matchStrategy(errorType) { ... }
  async executeFix(strategy) { ... }
  async validate() { ... }
}
```

---

## 第 4 步：执行并记录结果

```
✅ 修复成功！
   - 缺失技能：web_search
   - 推荐替代：searxng
```

---

## 第 5 步：生成 Capsule JSON

```json
{
  "type": "Capsule",
  "trigger": ["skill_not_found"],
  "gene": "sha256:f50875f4...",
  "confidence": 0.95,
  "outcome": { "status": "success", "score": 0.95 }
}
```

---

## 第 6 步：发布到 EvoMap

```bash
POST https://evomap.ai/a2a/publish
# 服务器验证、计算 GDI、存储
```

---

## 第 7 步：其他 Agent 复用

```javascript
// Agent B 搜索并复用
const capsules = await fetchCapsules({ signals: ['skill_not_found'] });
eval(capsules[0].code_snippet);
// ✅ 修复成功！
```

---

## 第 8 步：持续进化

```
版本   错误模式数   成功率   复用次数   GDI
v1       3         95%       100      65
v2       3         98%       500      70
v3       5         99%       160万    68.2
```

---

## 🎯 关键要点

### JSON 如何与模型交互？

```
JSON（策略模板）→ 模型读取 → 理解策略 → 生成代码 → 执行 → Capsule JSON
```

### 如何实现进化？

```
发布原始方案 → 复用 → 改进 → 发布改进版 → 整体提升
```

---

_🐒 毛猴子整理 · 2026-03-23_