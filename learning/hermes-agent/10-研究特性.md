# 10-研究特性

> Hermes 的研究特性：RL 训练、轨迹生成、Atropos 环境，为 AI Agent 研究提供工具。

---

## 第一步：概念解释

### Hermes 的研究能力

Hermes 不仅是应用工具，也支持 AI Agent 研究：

| 特性 | 说明 |
|------|------|
| **Batch Processing** | 批量生成轨迹 |
| **Trajectory Export** | 导出 ShareGPT 格式轨迹 |
| **RL Training** | 与 Atropos 集成，强化学习训练 |
| **Evaluation Framework** | Agent 评估环境 |

---

### 为什么 Hermes 有研究特性？

Hermes 由 **Nous Research** 开发——一个专注于 AI 模型训练的研究实验室：
- 开发 Hermes、Nomos、Psyche 模型
- 需要 Agent 轨迹数据训练下一代模型
- 提供研究工具支持社区研究

---

## 第二步：类比理解

### 轨迹 = Agent 的完整决策记录

轨迹（Trajectory）记录：
- 用户输入
- Agent 思考过程
- 工具调用
- 工具返回
- 最终响应

**类比：**

```
传统对话记录：
用户："检查服务器"
Agent："服务器正常"
（只记录输入输出）

轨迹：
用户："检查服务器"
Agent 思考："我需要 SSH 工具"
工具调用：terminal("ssh user@server 'systemctl status nginx'")
工具返回："active (running)"
Agent 思考："服务正常，我再检查 HTTP"
工具调用：web_extract("https://server")
...
Agent 决策："一切正常"
（完整决策链）
```

### RL 训练 = 从轨迹学习

```
监督学习：告诉 Agent 正确答案
RL 训练：让 Agent 从轨迹学习，优化决策

轨迹提供：
- 成功案例（奖励高）
- 失败案例（奖励低）
→ Agent 学会更好的决策策略
```

---

## 第三步：代码/实践

### Batch Runner

批量生成轨迹：

```bash
hermes batch run prompts.txt --output trajectories/
```

**用途：**
- 批量测试 Agent 行为
- 生成训练数据
- 评估不同模型表现

---

### Trajectory 格式

ShareGPT 格式：

```json
{
  "conversations": [
    {
      "from": "human",
      "value": "Check server status"
    },
    {
      "from": "gpt",
      "value": "I'll check the server...",
      "tool_calls": [
        {
          "name": "terminal",
          "arguments": {"command": "ssh user@server 'systemctl status nginx'"}
        }
      ]
    },
    {
      "from": "tool",
      "value": "active (running)"
    },
    {
      "from": "gpt",
      "value": "The server is healthy."
    }
  ]
}
```

---

### Atropos RL 环境

```bash
# 安装 Atropos 子模块
git submodule update --init tinker-atropos
uv pip install -e "./tinker-atropos"
```

**Atropos 特性：**
- RL 环境框架
- 多种工具调用解析器
- 支持轨迹压缩
- 奖励函数定义

---

### Evaluation Framework

```bash
# 在 environments/ 目录
python -m environments.evaluate --agent hermes --task set
```

**用途：**
- 评估 Agent 在特定任务的表现
- 对比不同模型
- 研究决策策略

---

### Trajectory 保存

Agent 自动保存轨迹（可选）：

```yaml
# ~/.hermes/config.yaml
trajectory:
  save_enabled: true
  output_dir: ~/.hermes/trajectories/
  format: sharegpt
```

---

### 数据生成流水线

```
┌─────────────────┐
│   Prompt Set    │  ← 测试用例集合
└─────────────────┘
        ↓
┌─────────────────┐
│   Batch Runner  │  ← 批量执行
└─────────────────┘
        ↓
┌─────────────────┐
│   Trajectories  │  ← 生成轨迹
│   (ShareGPT)    │
└─────────────────┘
        ↓
┌─────────────────┐
│   Atropos RL    │  ← RL 训练
└─────────────────┘
        ↓
┌─────────────────┐
│   New Model     │  ← 训练下一代模型
└─────────────────┘
```

---

## 第四步：知识关联

### 研究特性与架构

```
hermes-agent/
├── batch_runner.py        # 批量轨迹生成
├── agent/
│   └── trajectory.py      # 轨迹保存辅助
├── environments/          # RL 训练环境
│   └── atropos/           # Atropos 集成
│   └── evaluate.py        # 评估框架
└── tinker-atropos/        # Atropos 子模块
```

### 相关概念

| 概念 | 关系 |
|------|------|
| [架构详解](03-架构详解.md) | environments/ 目录 |
| [Skills](04-Skills系统.md) | 研究用 Skills（如 evaluation-skill） |

---

## 🎯 关键理解

1. **轨迹 = 完整决策链**：不只是输入输出
2. **RL 训练需要轨迹**：从成功/失败案例学习
3. **Batch Runner 批量生成**：大规模数据生成
4. **Atropos RL 环境**：Nous Research 的 RL 框架
5. **研究导向设计**：Hermes 是研究产物也是研究工具

---

## 📋 研究工作流

### 基础研究流程

```bash
# 1. 准备测试用例
cat prompts.txt
Check server status
Summarize Hacker News AI articles
Deploy Kubernetes app

# 2. 批量生成轨迹
hermes batch run prompts.txt --output trajectories/

# 3. 分析轨迹
python analyze_trajectories.py trajectories/

# 4. RL 训练（可选）
cd tinker-atropos
python train.py --data ../trajectories/
```

---

### 高级研究

```bash
# 评估不同模型
hermes batch run prompts.txt --model claude-3.5-sonnet
hermes batch run prompts.txt --model hermes-3
hermes batch run prompts.txt --model gpt-4o

# 对比结果
python compare_models.py --dir results/
```

---

## 🔗 官方资源

- [Atropos GitHub](https://github.com/NousResearch/atropos)
- [Nous Research](https://nousresearch.com)
- [Trajectory Format 文档](https://hermes-agent.nousresearch.com/docs/developer-guide/trajectory-format)

---

*费曼学习法文档 - Hermes Agent*

> 注：研究特性需要开发者级别理解，详见官方 Developer Guide。