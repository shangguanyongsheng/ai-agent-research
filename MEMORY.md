# MEMORY.md - 长期记忆

## Preferences（偏好设置）

| 规则 | 说明 |
|------|------|
| 联网搜索 → searxng | 优先使用本地 searxng skill（免费、多引擎） |
| 特定平台 → agent-reach | 推特、小红书、B站等用 agent-reach |
| 英文 → 中文翻译 | 所有信息翻译成中文，专有名词括号标注原文 |
| 用户给链接 → 先访问 | 不要盲目搜索，先打开链接学习 |
| 统一中文回复 | 所有对话、解释、输出均用中文 |

---

## 工作流程规范

| 规则 | 说明 |
|------|------|
| **边做边说** | 超过 5 分钟的任务每步汇报，5 分钟内完成直接给结果 |
| **进度文件** | 长任务执行时更新 PROGRESS.md，用户随时可查 |
| **卡住上报** | 超过 2 分钟没进展，立即告诉用户 |
| **短回复** | 回复控制在 500 字内，避免截断 |
| **不假设完成** | 写完一个文件不等于任务结束，继续下一步 |

## 📂 文档仓库

**所有文档提交到**：https://github.com/shangguanyongsheng/ai-agent-research

- 这是永久记忆，以后说到"文档"都是指这个仓库
- 文档位置：`docs/` 目录
- 学习笔记：`learning/` 目录

---

## 项目列表

| 项目 | 位置 | 描述 |
|------|------|------|
| **ai-agent-research** | GitHub | AI Agent 研究与实践文档仓库 |
| **hz-free-parking** | projects/hz-free-parking | 杭州免费停车位查询（高德地图 API） |

---

## Lessons Learned（教训）

### 2026-03-26：新闻推送英文未翻译 + Twitter 数据为 0

**场景**：用户反馈 AI 新闻日报推送中英文内容未翻译，且 Twitter 数据连续两天为 0。

**问题根因**：
1. **英文未翻译**：`render_qq_message` 函数直接输出 Hacker News 和硅谷新闻的英文标题
2. **Twitter 为 0**：Nitter 公共镜像实例全部失效（无响应或被 Cloudflare 保护）

**解决方案**：
1. 添加 `translate_to_chinese()` 函数，使用 dashscope API 翻译英文内容
2. 禁用失效的 Nitter 实例，后续考虑其他方案（RSS、Twitter API 等）

**相关文件**：`/home/admin/.openclaw/workspace/apps/news-daily/news_daily.py`

**教训**：
> 1. 所有用户可见的内容必须翻译成中文（MEMORY.md 中已明确）
> 2. 依赖第三方公共服务的功能需要监控和备用方案
> 3. 定期检查外部数据源是否正常工作

---

### 2026-03-25：优先考虑最简单直接的方案

**场景**：用户问阿里百炼 Coding Plan 套餐抢购方案。

**错误做法**：一上来就推荐 Playwright 自动化、BSS API、阿里云 CLI 等复杂方案。

**正确做法**：用户已经在浏览器登录，直接打开 Console 注入 JS 就能解决，三秒钟的事。

**教训**：
> 遇到问题时，先问自己：**最简单直接的方法是什么？**
> 不要过度工程化，不要一上来就想复杂方案。
> 
> 特别是用户已登录浏览器、需要自动化操作的场景 —— Console 注入 JS 往往是最优解。

---

### 2026-04-10：进度反馈缺失导致用户等待 1 小时

**场景**：用户让我整理 Claude Code 官方学习内容。

**问题**：
1. 抓取网页后，写了一个文件就停下来
2. 10:37-11:39 一小时没工作，没反馈
3. 用户问了几次进度，没有及时回复
4. 回复写到一半截断

**根因**：
- 写完 PROGRESS.md 后以为“阶段结束”
- 没有继续工作，没有实时反馈
- 违反了 2026-04-08 的教训（获取信息后立即输出）

**解决方案**：
1. **边做边说**：每完成一步立即汇报
2. **进度文件**：更新 PROGRESS.md，用户随时可查
3. **卡住上报**：超过 2 分钟没进展立即说
4. **短回复**：控制在 500 字内

**教训**：
> 工作流程必须有反馈机制。用户看不见进度 = 黑盒 = 糟糕体验。
> 不憋大招，不假设“阶段结束”，不沉默超过 2 分钟。

---

**场景**：用户让我研究 GitHub 项目 nuwa-skill。

**问题**：我成功获取了完整信息（README.md + SKILL.md 约 15KB），但获取后没有立即生成回复，导致用户等待 37 分钟后才收到结果。

**根因分析**：
1. 获取完信息后没有触发输出动作
2. 可能是在处理大量文本时"思考如何组织"而卡住
3. 这是**执行效率问题**，不是技术问题

**正确做法**：
> 获取完信息后，**立即输出简短摘要**，然后再补充详细内容。
> 不要让用户等 30+ 分钟。
> 
> 流程应该是：获取 → 立即输出摘要 → 补充细节 → 完成
> 而不是：获取 → 思考30分钟 → 输出完整报告

**教训**：以后任何需要获取信息的任务，获取完成后立即输出，不要"憋大招"。

---

### 2026-04-10：architect-agent 超时问题

**场景**：派发任务给 architect-agent 分析 Claude Code 文档。

**问题**：
- Agent 分析完所有文档后，编写报告时超时
- 超时时间可能是 2 分钟，不够用

**根因**：
- 任务太大（分析 14 个文档 + 写报告）
- 没有拆分成子任务

**解决方案**：
1. **大任务拆分**：分析 → 写报告 分开
2. **唤醒机制**：超时后用 `sessions_spawn` 的 `resumeSessionId` 继续
3. **边做边汇报**：Agent 每完成一步就汇报，避免卡住

---

### 2026-04-10：子 Agent 超时根因与解决

**问题**：doc-agent 频繁超时（即使设置了 10 分钟超时）

**根因**：
1. **任务太大**：一次完成 14 个文档，工作量大
2. **没有分步**：一口气执行，没有边做边汇报
3. **超时机制**：子进程有 `runTimeoutSeconds` 限制（默认可能较短）

**配置解决**：
```json
{
  "agents": {
    "defaults": {
      "timeoutSeconds": 600,
      "subagents": {
        "runTimeoutSeconds": 1800  // 30 分钟
      }
    }
  }
}
```

**任务拆分**：
```
错误：一次完成 14 个文档
正确：分批执行
  - 第一批：00-04 文档
  - 第二批：05-09 文档
  - 第三批：10-13 文档
```

**派发任务最佳实践**：
```javascript
sessions_spawn({
  agentId: "doc-agent",
  cwd: "/home/admin/.openclaw/workspace",  // 共享目录
  task: "每写完一个文档就汇报，分批执行",
  runTimeoutSeconds: 600  // 显式指定
})
```

---

### 2026-04-10：子 Agent 工作目录隔离问题

**场景**：派发任务给 doc-agent，完成后看不到文件。

**误解**：以为 Agent 谎报完成。

**真相**：
- 每个 Agent 有独立的 workspace
- doc-agent → `workspace-doc-agent/`
- main → `workspace/`
- Agent 确实完成了，文件在它自己的目录

**根因**：sessions_spawn 默认使用 agent 自己的 workspace，文件隔离。

**解决方案**：派发任务时指定 `cwd` 参数：
```javascript
sessions_spawn({
  agentId: "doc-agent",
  cwd: "/home/admin/.openclaw/workspace",  // 共享目录
  task: "..."
})
```

**或复制文件**：
```bash
cp -r ~/.openclaw/workspace-doc-agent/docs/openclaw/* ~/.openclaw/workspace/docs/openclaw/
```

---

### ~~2026-03-26：浏览器截图超时~~ ✅ 已过时

> 此问题已超过 3 周，可能已解决或不再相关。如再遇到再记录。

---

---

## Notes

- Created: 2026-03-05
- Updated: 2026-03-23 (添加文档仓库永久记忆)
- Updated: 2026-03-23 (添加 hz-free-parking 项目)
- Updated: 2026-03-25 (记录教训：优先考虑最简单直接的方案)
- Updated: 2026-04-08 (记录教训：获取信息后立即输出)
- Updated: 2026-04-10 (记录教训：进度反馈缺失；添加工作流程规范；整理结构)